<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Add Class + Calendar + Progress</title>
  <style>
    body {
      margin: 0;
      background-color: #f7f9fc;
      font-family: 'Segoe UI', sans-serif;
    }

    .alert {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 1000;
      padding: 12px 18px;
      border-radius: 6px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      font-size: 15px;
      animation: fadein 0.4s ease;
    }

    .alert-success {
      background: #d4edda;
      color: #155724;
      border-left: 5px solid #28a745;
    }

    .alert-error {
      background: #f8d7da;
      color: #721c24;
      border-left: 5px solid #dc3545;
    }

    @keyframes fadein {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .circular-progress {
      position: fixed;
      top: 200px;
      right: 200px;
      width: 80px;
      height: 80px;
    }

    .circular-progress svg {
      transform: rotate(-90deg);
    }

    .circular-progress circle {
      fill: none;
      stroke-width: 10;
    }

    .circular-progress .bg {
      stroke: #eee;
    }

    .circular-progress .progress {
      stroke: #3498db;
      stroke-dasharray: 251.2;
      stroke-dashoffset: calc(251.2 * (1 - 0.75));
      transition: stroke-dashoffset 0.5s ease;
    }

    .circular-progress .progress-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 16px;
      font-weight: bold;
      color: #3498db;
      pointer-events: none;
    }

    .calendar-button {
      position: fixed;
      bottom: 20px;
      right: 20px;
      padding: 12px 16px;
      background-color: #2ecc71;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
      transition: background-color 0.3s ease;
    }

    .calendar-button:hover {
      background-color: #27ae60;
    }

    .add-class-form {
      max-width: 340px;
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.12);
      position: fixed;
      top: 50%;
      left: 40px;
      transform: translateY(-50%);
    }

    .add-class-form label {
      display: block;
      font-size: 15px;
      font-weight: 600;
      color: #333;
      margin-bottom: 10px;
    }

    .add-class-form input {
      width: 100%;
      padding: 10px 14px;
      font-size: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
      margin-bottom: 15px;
    }

    .add-class-form button {
      display: flex;
      align-items: center;
      gap: 6px;
      background-color: #e67e22;
      color: white;
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
      transition: background 0.3s ease;
    }

    .add-class-form button:hover {
      background-color: #d35400;
    }

    .course-list {
      position: fixed;
      top: 20px;
      left: 420px;
      max-height: 500px;
      overflow-y: auto;
      width: 400px;
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
    }

    .course-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 0;
      border-bottom: 1px solid #eee;
      font-size: 15px;
    }

    .remove-btn {
      background: none;
      border: none;
      color: #e74c3c;
      font-weight: bold;
      cursor: pointer;
      font-size: 16px;
      transition: color 0.2s;
    }

    .remove-btn:hover {
      color: #c0392b;
    }
  </style>
</head>

<body>
  {{#if added}}
  <div class="alert alert-success">
    ✅ Course <strong>{{added}}</strong> was successfully added!
  </div>
  <script>
    setTimeout(() => document.querySelector('.alert-success').style.display = 'none', 5000);
  </script>
  {{/if}}

  {{#if message}}
  <div class="alert alert-error">{{message}}</div>
  <script>
    setTimeout(() => document.querySelector('.alert-error').style.display = 'none', 5000);
  </script>
  {{/if}}

  <div class="circular-progress">
    <svg width="80" height="80">
      <circle class="bg" cx="40" cy="40" r="40" />
      <circle class="progress" cx="40" cy="40" r="40" />
    </svg>
    <div class="progress-text">75%</div>
  </div>

  <button class="calendar-button" onclick="location.href='/calendar'">📅 Calendar</button>

  <form class="add-class-form" action="/add-class" method="POST">
    <label for="course_id">
      Enter Course Code <span style="color: #999;">(e.g., ACCT 3220)</span>:
    </label>
    <input type="text" name="course_id" placeholder="ACCT 3220" required />
    <button type="submit">
      <span style="font-size: 18px;">➕</span> Add Class
    </button>
  </form>

  {{#if courses}}
  <div class="course-list">
    <h3 style="margin-top: 0;">Your Courses</h3>
    {{#each courses}}
    <div class="course-item">
      <span>{{this.course_id}} - {{this.course_name}}</span>
      <form method="POST" action="/remove-class" style="margin: 0;">
        <input type="hidden" name="course_id" value="{{this.course_id}}">
        <button type="submit" class="remove-btn">✕</button>
      </form>
    </div>
    {{/each}}
  </div>
  {{/if}}

</body>

</html>